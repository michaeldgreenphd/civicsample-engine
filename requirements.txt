# Weekly demographics pipeline (src/ + scripts/*.py).
# The LLM extraction stack has its own file: scripts/extraction/requirements.txt
requests>=2.31.0
rapidfuzz>=3.0.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0
